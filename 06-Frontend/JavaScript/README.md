# JavaScript

**Week 8, Days 3–6** · [← Frontend](../README.md)

## 📅 Week 8 Tasks
- [ ] Day 3 — Basics: `let`/`const`/`var`, data types, `==` vs `===`, functions and arrow functions, template literals, arrays and objects → `01-js-basics.md`
- [ ] Day 4 — Core concepts: scope, hoisting, closures, `this`, destructuring, spread/rest, array methods (`map`, `filter`, `reduce`, `find`), ES modules → `02-js-core.md`
- [ ] Day 5 — DOM & events: `querySelector`, creating elements, event listeners, event bubbling and delegation, `localStorage` → build a to-do list in `todo-app/`
- [ ] Day 6 — Async: the event loop, callbacks, promises, `async`/`await`, `fetch` with error handling → `03-async-js.md`; load and show data from a public API such as JSONPlaceholder in `todo-app/`

## 🎯 Must-Know for Interviews
- Closures, with an example you wrote
- The event loop: call stack, microtasks, macrotasks
- `==` vs `===`; `null` vs `undefined`
- `var` vs `let` vs `const`
